spring:
  application:
    name: students-david
  datasource:
    url: jdbc:h2:mem:studentsdb_david
    driver-class-name: org.h2.Driver
    username: admin
    password: admin
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8082
